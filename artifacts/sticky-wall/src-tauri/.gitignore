# Cargo build output — large, machine-specific, regenerated on every
# `tauri dev` / `tauri build`.
target/

# Tauri-generated capability schemas + acl artifacts. Regenerated by
# `tauri-build` on every Cargo build. Safe to ignore.
gen/

# Local Cargo override knobs (rarely needed; keep out of repo).
.cargo/config.toml
